
import React, { useEffect, useState, useCallback, useMemo, useRef } from 'react-native';
import { Button, Text, View, FlatList, StyleSheet, ActivityIndicator, StatusBar, SafeAreaView, ScrollView, Box, TouchableWithoutFeedback  } from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';
const ProfileComponent = () => {
  return (
    <Text>Profile</Text>
  )
}

export default ProfileComponent